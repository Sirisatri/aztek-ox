# คู่มือการติดตั้งและตั้งค่า OX Game

## 📋 สิ่งที่ต้องเตรียม

1. Node.js (เวอร์ชัน 18 หรือสูงกว่า)
2. npm หรือ yarn
3. บัญชี Google (สำหรับ OAuth)
4. บัญชี Supabase (ฟรี)

## 🔧 ขั้นตอนการติดตั้ง

### ขั้นตอนที่ 1: ติดตั้ง Dependencies

```bash
cd aztek
npm install
```

### ขั้นตอนที่ 2: ตั้งค่า Google OAuth

1. เปิด [Google Cloud Console](https://console.cloud.google.com/)
2. สร้างโปรเจคใหม่:
   - คลิก "Select a project" > "New Project"
   - ตั้งชื่อโปรเจค เช่น "OX Game"
   - คลิก "Create"

3. เปิดใช้งาน Google+ API:
   - ไปที่ "APIs & Services" > "Library"
   - ค้นหา "Google+ API"
   - คลิก "Enable"

4. สร้าง OAuth 2.0 Credentials:
   - ไปที่ "APIs & Services" > "Credentials"
   - คลิก "Create Credentials" > "OAuth client ID"
   - เลือก Application type: "Web application"
   - ตั้งชื่อ: "OX Game Web Client"
   - เพิ่ม Authorized redirect URIs:
     ```
     http://localhost:3000/api/auth/callback/google
     ```
   - คลิก "Create"
   - **บันทึก Client ID และ Client Secret**

### ขั้นตอนที่ 3: ตั้งค่า Supabase

1. เปิด [Supabase](https://supabase.com/) และสร้างบัญชี (ถ้ายังไม่มี)

2. สร้างโปรเจคใหม่:
   - คลิก "New Project"
   - ตั้งชื่อโปรเจค เช่น "ox-game"
   - ตั้งรหัสผ่านฐานข้อมูล (บันทึกไว้)
   - เลือก Region ที่ใกล้คุณที่สุด (เช่น Southeast Asia)
   - คลิก "Create new project"
   - รอประมาณ 2-3 นาทีให้โปรเจคเสร็จ

3. รัน SQL Schema:
   - ไปที่ "SQL Editor" ในแถบด้านซ้าย
   - เปิดไฟล์ `supabase-schema.sql` ในโปรเจค
   - คัดลอกเนื้อหาทั้งหมด
   - วางใน SQL Editor ของ Supabase
   - คลิก "Run" หรือกด Ctrl+Enter
   - ตรวจสอบว่ามีข้อความ "Success. No rows returned"

4. ดึงข้อมูล API Keys:
   - ไปที่ "Settings" (ไอคอนเฟือง) > "API"
   - **บันทึกค่าเหล่านี้**:
     - Project URL: `https://xxxxx.supabase.co`
     - anon/public key (ในส่วน Project API keys)

### ขั้นตอนที่ 4: สร้างไฟล์ .env.local

1. คัดลอกไฟล์ตัวอย่าง:
```bash
cp .env.example .env.local
```

2. แก้ไขไฟล์ `.env.local`:
```env
# สร้าง AUTH_SECRET
AUTH_SECRET=<รันคำสั่ง npx auth secret และคัดลอกผลลัพธ์มาวางที่นี่>

# Google OAuth (จากขั้นตอนที่ 2)
AUTH_GOOGLE_ID=your-google-client-id.apps.googleusercontent.com
AUTH_GOOGLE_SECRET=GOCSPX-xxxxxxxxxxxxx

# Supabase (จากขั้นตอนที่ 3)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxx
```

3. สร้าง AUTH_SECRET:
```bash
npx auth secret
```
คัดลอกค่าที่ได้มาวางในไฟล์ `.env.local`

### ขั้นตอนที่ 5: รันแอปพลิเคชัน

```bash
npm run dev
```

เปิดเบราว์เซอร์ที่ http://localhost:3000

## ✅ ทดสอบการทำงาน

### 1. ทดสอบ Authentication
- เปิด http://localhost:3000
- คลิก "Login" หรือไปที่ http://localhost:3000/login
- คลิก "Sign in with Google"
- เลือกบัญชี Google
- ตรวจสอบว่าเข้าสู่หน้า Dashboard สำเร็จ

### 2. ทดสอบเกม
- ที่หน้า Dashboard คลิกช่องว่างเพื่อเล่นเกม
- ตรวจสอบว่าบอททำงานและเล่นตอบกลับ
- เล่นจนจบเกม
- ตรวจสอบว่าคะแนนอัปเดตถูกต้อง

### 3. ทดสอบกระดานคะแนน
- คลิกปุ่ม "🏆 กระดานคะแนน"
- ตรวจสอบว่าเห็นชื่อและคะแนนของคุณ

## 🔍 การแก้ไขปัญหาที่พบบ่อย

### ปัญหา: "Error: AUTH_SECRET is not set"
**วิธีแก้**: 
```bash
npx auth secret
```
คัดลอกค่าที่ได้ไปวางในไฟล์ `.env.local` ที่บรรทัด `AUTH_SECRET=`

### ปัญหา: "NEXT_PUBLIC_SUPABASE_URL is not defined"
**วิธีแก้**: ตรวจสอบว่าได้ใส่ Supabase URL และ ANON_KEY ในไฟล์ `.env.local` แล้ว

### ปัญหา: Google OAuth Redirect Error
**วิธีแก้**: 
1. ตรวจสอบว่า Authorized redirect URIs ใน Google Cloud Console ตรงกับ:
   ```
   http://localhost:3000/api/auth/callback/google
   ```
2. ตรวจสอบว่า AUTH_GOOGLE_ID และ AUTH_GOOGLE_SECRET ถูกต้อง

### ปัญหา: ไม่สามารถบันทึกคะแนนได้
**วิธีแก้**:
1. ตรวจสอบว่าได้รัน SQL schema ใน Supabase แล้ว
2. ไปที่ Supabase Dashboard > Table Editor
3. ตรวจสอบว่ามีตาราง `user_scores` และ `game_history`
4. ถ้าไม่มี ให้รัน SQL จากไฟล์ `supabase-schema.sql` อีกครั้ง

### ปัญหา: เกมไม่แสดงหรือ Error ในเบราว์เซอร์
**วิธีแก้**:
1. เปิด Developer Tools (F12)
2. ดูที่แท็บ Console หาข้อความ error
3. ตรวจสอบว่าได้ติดตั้ง dependencies ครบ:
   ```bash
   npm install
   ```
4. ลองรีสตาร์ท dev server:
   ```bash
   # กด Ctrl+C เพื่อหยุด
   npm run dev
   ```

## 🚀 Deploy to Production

### Vercel (แนะนำ)

1. Push code ไปยัง GitHub
2. ไปที่ [Vercel](https://vercel.com)
3. Import GitHub repository
4. เพิ่ม Environment Variables ทั้งหมดจาก `.env.local`
5. อัปเดต Google OAuth Redirect URI:
   ```
   https://your-app.vercel.app/api/auth/callback/google
   ```
6. Deploy!

### Environment Variables สำหรับ Production
ใน Vercel Dashboard > Settings > Environment Variables เพิ่ม:
- `AUTH_SECRET`
- `AUTH_GOOGLE_ID`
- `AUTH_GOOGLE_SECRET`
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`

## 📊 ตรวจสอบข้อมูลใน Supabase

1. ไปที่ Supabase Dashboard
2. คลิก "Table Editor"
3. เลือกตาราง `user_scores` เพื่อดูคะแนนผู้เล่น
4. เลือกตาราง `game_history` เพื่อดูประวัติการเล่น

## 🎯 การใช้งานต่อ

- เพิ่ม OAuth providers อื่นๆ (GitHub, Facebook, etc.)
- ปรับแต่งระดับความยากของบอท
- เพิ่มโหมดผู้เล่น vs ผู้เล่น
- เพิ่มระบบ ranking ประจำเดือน
- เพิ่มระบบ achievement/badges

## 📞 ต้องการความช่วยเหลือ?

หากมีปัญหาหรือข้อสงสัย:
1. ตรวจสอบ Console logs (F12 > Console)
2. ตรวจสอบ Terminal output
3. ตรวจสอบไฟล์ `.env.local` ว่าครบถ้วน
4. อ่าน error message อย่างละเอียด
